const myQuote = "Im a french bot :>";
const mySignature = "Pwned by a french bot :>";
const myBotName = "[BOT]sNapz_1";

// Change the way to pick a victim, 0 = random || 1 = Focus the last || 2 = Focus the first
const findVictimeType = 1;
